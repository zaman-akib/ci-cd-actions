### Repo for fetching GitHub commits and pushing docker image to Docker Hub

### Purpose 
  - Fetch GitHub commits and print details
  - Dockerize the script
  - Build the docker image and push it to Docker Hub
